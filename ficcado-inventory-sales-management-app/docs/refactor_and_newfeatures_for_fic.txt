these below are updates which we need to make on the existing code and I have also added new features which we need to implement in the application before going live:

refactor on existing logics:

1: the current activity log messages are good but not easy to understand we need a proper understandable message in the activity log for any transactions or activity happened throughout the application, we must give a meaning full and user understandable message it okey if its a long message the message should be at least 180 to 250 char.

2: since we have implemented the download and send mail confirmation feature into the application sales section now we must also track the record in the activity log where when an admin downloads or send a confirmation mail for a sale it must be tracked in the activity log too.

3: in the sales section when creating a new sale set the default payment status to not paid

4: currently our brand logo is placed at 'E:\Ficcado\Ficcado_inventory-sales_management_app\ficcado-inventory-sales-management-app\assets\Images\ficcado_logo.jpg' there were a miscommunication that we mentioned it as a lizard but in fact the logo is of 'Gecko' so we must update the loading animation and change it to a Gecko and the animation should be like its walking forwards for 2  to 5 seconds and turning around and walking back for 2 to 5 seconds this gets repeated till loading ends

5: currently we have a feature in the admin control for Download Reports and Email Reports so currently we have only: Download Reports:
📊 Items Management
📊 Inventory Management
📊 Warehouse Management
📊 Sales Management
📊 Replacement Management
📊 Return & Refund Management

but there are more sheets which were not yet included in this section so we must include all of them in our download report(excluding the admin information sheet). in the same way when admin clicks the ✉️ Email Reports and clicks the send full report now we must all send the remaining new sheets . we need all the sheets to be send to the admin (excluding the admin information sheet)

6: in the admin's my profile section the phone number is not showing inside the field even after value exists. make sure the value is displayed on the field.

7: in the admin control section currenlty the admins lists all the admin and we have given the delete button but now the changes are remove the delete button and repalce it with view/edit button and when admin clicks it we show the admins details and provide a edit faeture in it. along with that we show informations like how many sale have this admin created and how many are closed. and the total revenue generated by this admin.

8: currently in our onboarding section the superadmin was asked to create a new/first admin but that admin and the details were not saved any where? also the super admin was not even asked to create a password for that first admin so when superadmin created the first admin the first admin must be added in the admin information sheet considering it as a create.




new features: 

1: recently we have implemented the customer information collection where we saves the customers information so here we must provide features like view/edit and must have a dedicated view/edit page for this feature and inside the page we must properly showcase the customers information and a customer order history section in the same page . here in the order history of each customer we must show all the orders that customer had purchases and we must show information's like invoice number, items name, sizes, discount applied, total amount, order placed date and purchase completed date, mode of payment, any pending amount(if all paid show No Dues, if yes then show payment pending), and who created the sale and who completed the sale.

2: so when a admin is logged in and when he/she is logged for 30 min without logout the admin should recieve a popup in the screen asking are you still online? and along with that we give a yes button and if admins clicks the button the session continues and along with that a 30 sec timer runs so if the admin didn't respond in 30sec he will be automatically logged out. this way we can keep the server cool and speed for new or current using admins. the main agenda of this is to provide a fast request and response through out the application for all the logged in admins. 

3: so currently we are sending confirmation mails to customer so we need an update on the message that was send , so we must also mention the discounts applied to that order if no discount is applied skip the row and if yes include it also maintain a proper table to show the customer what they have purchased and what size and the total amount charged for that order. making and keeping a professional touch in the confirmation mails

4: currently when a note is created we are facing error:
## Error Type
Console Error

## Error Message
Each child in a list should have a unique "key" prop.

Check the render method of `NotesPage`. See https://react.dev/link/warning-keys for more information.


    at div (<anonymous>:null:null)
    at eval (app\(app)\dashboard\notes\page.tsx:139:13)
    at Array.map (<anonymous>:null:null)
    at NotesPage (app\(app)\dashboard\notes\page.tsx:138:18)

## Code Frame
  137 |         <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
  138 |           {notes.map((note) => (
> 139 |             <div key={note.sno} className="card" style={{ padding: '14px 16px' }}>
      |             ^
  140 |               {editingNote?.sno === note.sno ? (
  141 |                 <form onSubmit={handleEditNote}>
  142 |                   <textarea

Next.js version: 16.3.0 (Webpack)



also after fixing it I need you to bring a the Update and delete option for the created notes. but make sure the creator can only update and delete the note



part 2: new requirements:

1: currently am impressed with when admin clicks the customer and instantly the screen loads and show the ui but after that it takes time to show the data here when a user is using the application this approach never irritates the user or admin because he chose a screen and instantly the screen appears and after that it takes time to load data here the data loading is fetching and loading behind and the user/admin doesn't need to wait for that from the time the request is send instead he gets the screen and shows that the data's are fetching or loading. I need this approach to be made on all over the application , without skipping any section, implement this stratergy all over the application, in all pages!.

2: there is a huge mistake with the keep notes section where there are 2 input fields for the user and when some admin adds a note and clicks the 'add note' and instantly the input field gets multiplied and no notes are shown.

3: currently in the my profile section we need the change password option/feature for admins so implement the change password feature




serious troubles found and requires help and fixes:

1: when I was manually testing the replacement logic and its connections I came to find that :
when a replacement was made and the items are camera- blue size m quantity 2 and eternity- black size m quantity 1 and here this was the items ordered. so now the customer requested for a replacement and admin updates the replacement but after updating the replacement request I came to see that admin have already updated in ui about which item were requested to replace and with what it wants to be replaced with so here in the sheets we not collecting information's like what was the selected item for replacement ,what was the size of that item and how many of them were requested to replace (the total count of item), and In this scenario we have camera- blue size m quantity 2 and eternity- black size m quantity 1 and customer requested to replace one camera-blue size m with a maharajas-black size Xl. so we need a clear end to end datas to be saved in the sheets on what ,how many, which etc everything should be recorded properly in the sheets and those datas should be taken to display if tried to view in any other time:

the current columns are : S.No	Invoice Number	Total Number of Items Purchased	Last Purchased Item(s)	Last Purchased Item(s) Size	New Item(s)	New Item(s) Size	Invoice Status	Disposition of Old Items	Restock Destination	Created At

find me solution for this situation, create a plan







So there is a huge mistake hidden under the replacement logic we had made which was:
when a replacement was created admin goes to the manage/update and performs the changes and clicks save progress. here as soon as the save progress was made all the transaction in the inventory are made the restocking, adding and removing etc everything happens at that point and then admin goes back and comes back again he gets all the new stocks needed to be given to customer and here at this point all the values are changes and set into a different value where previous save progress were gone 


